apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "common-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "common-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "common-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
